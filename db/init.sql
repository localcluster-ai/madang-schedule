CREATE TABLE IF NOT EXISTS schedules (id BIGINT AUTO_INCREMENT PRIMARY KEY, title VARCHAR(255) NOT NULL, content TEXT, start_time DATETIME, end_time DATETIME, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
